import { DefaultTheme } from "@react-navigation/native";

export const MonoLight = {
    dark: false,
    ...DefaultTheme,
    colors: {
        background: 'rgb(255, 255, 255)',
        text: 'rgb(0, 0, 0)',
    }
}